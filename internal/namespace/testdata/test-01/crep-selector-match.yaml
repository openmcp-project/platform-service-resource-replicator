apiVersion: core.open-control-plane.io/v1alpha1
kind: ClusterReplica
metadata:
  name: crep-selector-match
spec:
  sources:
    - id: src
      apiVersion: v1
      kind: ConfigMap
      name: source-cm
      namespace: default
  targets:
    - namespace:
        selector:
          matchLabels:
            env: test
