apiVersion: core.open-control-plane.io/v1alpha1
kind: Replica
metadata:
  name: rep-nomatch
  namespace: default
spec:
  sources:
    - id: src
      apiVersion: v1
      kind: ConfigMap
      name: source-cm
      namespace: default
  targets:
    - namespace:
        selector:
          matchLabels:
            env: prod
